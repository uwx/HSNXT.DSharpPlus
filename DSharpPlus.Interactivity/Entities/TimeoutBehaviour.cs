namespace DSharpPlus.Interactivity
{
    public enum TimeoutBehaviour
    {
        // is this actually needed?
        //Default, // ignore
        Ignore,
        DeleteReactions,
        DeleteMessage
    }
}